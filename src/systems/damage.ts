import * as state from '../state';

export function run(
    attacker: state.Component<'attacker'>,
    defender: state.Component<'health'>,
    attackCardId: string,
    defendCardId: string,
): void {

}
