name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run lint
      run: |
        npm ci
        npx tsc --noEmit
        npx eslint --ext ".ts" src
